# system：Agent 入口

## 学习知识库任务

用户要求整理书本/OCR/笔记、归纳可背诵知识、插入或修改学习卡片、维护章节树、理解知识图与卡片关系、使用 study-kb 前端或复习时：

1. 阅读 `.cursor/skills/study/SKILL.md`；它已包含完整 Agent 操作手册，无需再读另一份 AGENT.md。
2. 向用户解释使用方法时参考根 `README.md` 的“知识交互完整使用指南”。
3. 默认学习库为 `study-kb/data/study.sqlite`。先核对实际路径、已有节点 ID 和用户确认范围，再写入；不能用整书重复导入代替已有树增量维护。
4. 写知识时保留来源、父子关系、卡片关联和既有复习历史；只有用户实际复习评分才写正式评分记录。
5. 已确认草稿与目标范围可直接执行；仅要求总结或文档时不得插入真实学习数据。

此路由是为了让不能自动发现 `.cursor/skills/` 的 agent 也使用同一套 skill，不维护另一份重复技能。

## 仓库边界

- 改动前检查 Git 状态，保留用户未提交的修改。
- `3dStudio/` 是共享 UI 主源链接；不要把学习数据写入其 `ops.sqlite`。
- `readme/` 是 Rsim_helper 中央文档池链接；system 的用户说明放根 `README.md`，学习执行说明统一放 `.cursor/skills/study/SKILL.md`。
- 针对共享链接目标的开发，另行读取目标仓库适用的 `AGENTS.md`，不把本仓库授权当作任意修改共享项目的授权。
